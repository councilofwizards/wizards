{
  "name": "wizards",
  "version": "1.1.0",
  "private": true,
  "description": "A Claude Code plugin marketplace for orchestrating AI agent teams to plan, build, and operate SaaS products.",
  "scripts": {
    "prepare": "husky"
  },
  "lint-staged": {
    "*.{md,json,yml,yaml}": "prettier --write"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0",
    "prettier": "^3.8.1"
  }
}
